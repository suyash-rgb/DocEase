spring:
  application:
    name: aiservice

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761//eureka/

# --will add later
#eureka:
#  client:
#    register-with-eureka: true
#    fetch-registry: true