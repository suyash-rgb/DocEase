feat(ai-doctor-finder): complete end-to-end AI specialist + real doctors with slots

MASSIVE MVP MILESTONE – FULL FLOW WORKING:
- Added AppointmentSlot entity + real slots logic
- Extended Doctor with city, clinic, rating, reviews
- Fixed Hibernate 6 scale error on Double fields
- Native query for available doctors + tomorrow slots with GROUP_CONCAT
- Built full SpecialistSuggestionService: Gemini → user-service → response
- WebClient integration with proper host/port/path for local dev
- Removed Lombok, fixed WebClient bean conflicts with @Qualifier
- Added AiController + /ai/suggest-specialist endpoint
- Full DTO sync + error handling + async logging
- End-to-end tested: AI understands symptoms → returns real doctors with real slots

THE AI DOCTOR FINDER IS ALIVE. FULLY. NO EXCUSES.

Ready for JWT + Gateway + Docker → LAUNCH SEQUENCE INITIATED.