fix(user-service): resolved native query ClassCastException for doctorId

- Changed findAvailableDoctors() to use interface-based projection (AvailableDoctorResponse)- kept the same name and instead created a constructor
- Replaced record constructor result mapping with Spring Data JPA interface projection
- Eliminates Hibernate 6 + MySQL JDBC driver bug where INT columns are returned as Long in native queries
- No changes required in aiservice — DTO/record remains unchanged and fully compatible
- Query now safely maps doctor_id (INT) → Integer doctorId without casting issues
- GROUP_CONCAT with ORDER BY preserved, slots display correctly

Root cause: Hibernate 6 sometimes returns Long even for INT columns in native queries when using constructor expressions.

Fix: Interface projection is the canonical, bulletproof solution for complex native queries in Spring Boot 3+.

The symptom based specialist finder is now working.

Tested end-to-end: "chest pain" → Cardiologist → real doctors + real slots returned successfully.