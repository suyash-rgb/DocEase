feat: Add global CORS configuration for aiservice

Implemented global CORS configuration to allow frontend requests from http://localhost:5173.

Key changes:
- Added CorsConfig class in aiservice to enable CORS for all endpoints.
- Configured CORS to support GET, POST, PUT, DELETE, OPTIONS methods with credentials.
- Added aiService service on the frontend.
- Integrated getPrecautionsAndRemedies() API call in ChatbotPopup.jsx
- Added structured rendering for nested symptom data(precautions+remedies)
- Implemented formatBotResponse() to display grouped results per symptom
- Improved error handling with falllback message block
- Updated message rendering to support JSX blocks from bot responses
- Minor UI tweaks: spacing, scroll behavior, laoding indicator

This centralizes CORS handling in aiservice, preparing for gateway-level CORS in production.